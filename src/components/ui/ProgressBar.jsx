export default function ProgressBar({ value = 0, className = '' }) { const pct = Math.min(100, Math.max(0, value)); return <div className={`h-3 overflow-hidden rounded-full bg-white/10 ${className}`} role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow={pct}><div className="h-full rounded-full bg-gradient-to-r from-anifine-primary to-anifine-accent transition-all duration-500" style={{ width: `${pct}%` }} /></div>; }
