import { AnimatePresence, motion } from 'framer-motion';
import { useEffect, useState } from 'react';
const particles = Array.from({ length: 22 }, (_, i) => ({ id: i, left: `${(i * 37) % 100}%`, top: `${(i * 61) % 100}%`, delay: (i % 7) * .25 }));
export default function SplashGate({ children }) { const [show, setShow] = useState(() => sessionStorage.getItem('anifine:splash') !== 'shown'); useEffect(() => { if (!show) return; const timer = setTimeout(() => { sessionStorage.setItem('anifine:splash', 'shown'); setShow(false); }, 2500); return () => clearTimeout(timer); }, [show]); return <><AnimatePresence>{show && <motion.button aria-label="Skip splash" type="button" onClick={() => { sessionStorage.setItem('anifine:splash', 'shown'); setShow(false); }} className="fixed inset-0 z-[100] grid place-items-center overflow-hidden bg-anifine-bg text-center" initial={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: 0.7, ease:'easeInOut' }}><motion.div className="absolute -left-24 top-10 h-[34rem] w-[34rem] rounded-full bg-anifine-primary/25 blur-3xl" animate={{ scale: [1, 1.14, 1], x:[0,30,0], opacity: [0.32, 0.55, 0.32] }} transition={{ duration: 5, repeat: Infinity }}/><motion.div className="absolute -right-20 bottom-0 h-[28rem] w-[28rem] rounded-full bg-amber-400/10 blur-3xl" animate={{ scale: [1.1, 1, 1.1], y:[0,-28,0], opacity: [0.2, 0.36, 0.2] }} transition={{ duration: 6, repeat: Infinity }}/>{particles.map(p=><motion.span key={p.id} className="absolute h-1 w-1 rounded-full bg-white/35" style={{left:p.left, top:p.top}} animate={{ y:[0,-26,0], opacity:[0,.55,0] }} transition={{duration:4.5, delay:p.delay, repeat:Infinity}}/>)}<motion.div className="relative px-10 py-12" initial={{ scale: .94, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ duration: .9, ease:'easeOut' }}><motion.h1 className="font-display text-7xl font-black tracking-[.16em] md:text-9xl" animate={{ scale:[1,1.018,1] }} transition={{duration:2.4, repeat:Infinity}}>ANIFINE</motion.h1><motion.p initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{delay:.45}} className="mt-5 text-lg lowercase tracking-[.35em] text-white/55">by nexus</motion.p></motion.div></motion.button>}</AnimatePresence>{children}</>; }
