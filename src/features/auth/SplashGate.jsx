import { AnimatePresence, motion } from 'framer-motion';
import { useEffect, useState } from 'react';
export default function SplashGate({ children }) { const [show, setShow] = useState(() => sessionStorage.getItem('anifine:splash') !== 'shown'); useEffect(() => { if (!show) return; const timer = setTimeout(() => { sessionStorage.setItem('anifine:splash', 'shown'); setShow(false); }, 2400); return () => clearTimeout(timer); }, [show]); return <><AnimatePresence>{show && <motion.button type="button" onClick={() => { sessionStorage.setItem('anifine:splash', 'shown'); setShow(false); }} className="fixed inset-0 z-[100] grid place-items-center overflow-hidden bg-anifine-bg text-center" initial={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: 0.6 }}><motion.div className="absolute h-[34rem] w-[34rem] rounded-full bg-anifine-primary/25 blur-3xl" animate={{ scale: [1, 1.16, 1], opacity: [0.45, 0.7, 0.45] }} transition={{ duration: 2.4, repeat: Infinity }}/><div className="glass relative rounded-[2rem] px-10 py-12"><motion.h1 initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="font-display text-6xl font-extrabold tracking-[.18em] md:text-8xl">ANIFINE</motion.h1><p className="mt-4 text-lg tracking-[.3em] text-white/60">By Nexus</p></div></motion.button>}</AnimatePresence>{children}</>; }
