import { Link, useNavigate } from 'react-router-dom';
import { useState } from 'react';
import Button from '../../components/ui/Button';
import GlassCard from '../../components/ui/GlassCard';
import Input from '../../components/ui/Input';
import { useAuth } from './AuthContext';
export function Login(){return <AuthForm mode="login"/>} export function Register(){return <AuthForm mode="register"/>}
function AuthForm({ mode }) { const isRegister = mode === 'register'; const { login, register } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ username: '', email: '', password: '' }); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async e => { e.preventDefault(); setBusy(true); setError(''); try { isRegister ? await register(form) : await login(form); navigate('/'); } catch (err) { setError(err.message); } finally { setBusy(false); } }; return <div className="mx-auto grid min-h-[70vh] max-w-md place-items-center"><GlassCard className="w-full"><p className="text-sm uppercase tracking-[.3em] text-anifine-accent">ANIFINE ACCESS</p><h1 className="mt-3 font-display text-4xl font-extrabold">{isRegister ? 'Create Account' : 'Login'}</h1><form onSubmit={submit} className="mt-7 space-y-4">{isRegister && <Input required label="Username" value={form.username} onChange={e=>setForm({...form, username:e.target.value})}/>}<Input required type="email" label="Email" value={form.email} onChange={e=>setForm({...form, email:e.target.value})}/><Input required type="password" minLength="6" label="Password" value={form.password} onChange={e=>setForm({...form, password:e.target.value})}/>{error && <p className="rounded-2xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<Button className="w-full" isLoading={busy}>{isRegister ? 'Register' : 'Login'}</Button></form><p className="mt-5 text-center text-sm text-white/55">{isRegister ? 'Already have an account?' : 'Need an account?'} <Link className="font-bold text-anifine-accent" to={isRegister ? '/login' : '/register'}>{isRegister ? 'Login' : 'Register'}</Link></p></GlassCard></div>; }
