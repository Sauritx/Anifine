import { Activity, Flame, Gamepad2, Star, Target, Trophy } from 'lucide-react';
import Avatar from '../components/ui/Avatar'; import GlassCard from '../components/ui/GlassCard'; import StatCard from '../components/ui/StatCard'; import XPBar from '../components/ui/XPBar';
import { useAuth } from '../features/auth/AuthContext'; import { xpToNextLevel } from '../features/gameplay/gameEngine';
export default function Profile(){ const { profile }=useAuth(); return <div className="space-y-6"><GlassCard><div className="flex items-center gap-5"><Avatar name={profile.username} className="h-20 w-20 text-2xl"/><div><h1 className="font-display text-4xl font-bold">{profile.username}</h1><p className="text-white/55">{profile.email}</p></div></div><div className="mt-6"><XPBar xp={profile.xp} nextLevelXp={xpToNextLevel(profile.level)} level={profile.level}/></div></GlassCard><div className="grid gap-4 md:grid-cols-3"><StatCard icon={Star} label="Points" value={profile.points}/><StatCard icon={Activity} label="XP" value={profile.xp}/><StatCard icon={Target} label="Accuracy" value={`${profile.accuracy}%`}/><StatCard icon={Gamepad2} label="Games Played" value={profile.gamesPlayed}/><StatCard icon={Flame} label="Current Streak" value={profile.currentStreak}/><StatCard icon={Trophy} label="Highest Streak" value={profile.highestStreak}/></div></div> }
