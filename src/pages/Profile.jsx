import { Activity, Flame, Gamepad2, Star, Target, Trophy, User } from 'lucide-react';
import Card from '../components/Card'; import StatCard from '../components/StatCard'; import ProgressBar from '../components/ProgressBar'; import { useAuth } from '../context/AuthContext'; import { xpToNextLevel } from '../utils/game';
export default function Profile(){const {profile}=useAuth(); const next=xpToNextLevel(profile.level); return <div className="space-y-6"><Card><div className="flex items-center gap-5"><div className="rounded-3xl bg-anifine-primary p-5"><User size={34}/></div><div><h1 className="font-display text-4xl font-bold">{profile.username}</h1><p className="text-white/55">{profile.email}</p></div></div><div className="mt-6"><div className="mb-2 flex justify-between text-sm text-white/60"><span>Level {profile.level}</span><span>{profile.xp} / {next} XP</span></div><ProgressBar value={(profile.xp/next)*100}/></div></Card><div className="grid gap-4 md:grid-cols-3"><StatCard icon={Star} label="Points" value={profile.points}/><StatCard icon={Target} label="Accuracy" value={`${profile.accuracy}%`}/><StatCard icon={Gamepad2} label="Games Played" value={profile.gamesPlayed}/><StatCard icon={Flame} label="Current Streak" value={profile.currentStreak}/><StatCard icon={Trophy} label="Highest Streak" value={profile.highestStreak}/><StatCard icon={Activity} label="XP" value={profile.xp}/></div></div>}
