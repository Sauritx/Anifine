import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Flame, Gamepad2, Star, Target, Trophy, User, Zap } from 'lucide-react';
import GlassCard from '../components/ui/GlassCard'; import StatCard from '../components/ui/StatCard'; import XPBar from '../components/ui/XPBar'; import Badge from '../components/ui/Badge';
import { useAuth } from '../features/auth/AuthContext'; import { xpToNextLevel } from '../features/gameplay/gameEngine';
export default function Home(){ const { profile }=useAuth(); const rank = profile.points > 5000 ? 'Elite' : profile.points > 2000 ? 'Rising Star' : 'Rookie'; const stats=[[Zap,'Current Level',profile.level],[Star,'Total Points',profile.points],[Trophy,'Current Rank',rank],[Gamepad2,'Games Played',profile.gamesPlayed],[Flame,'Current Streak',profile.currentStreak],[Target,'Highest Streak',profile.highestStreak]]; return <div className="space-y-8"><GlassCard className="overflow-hidden p-8 md:p-10"><Badge>Command Center</Badge><p className="mt-5 text-white/55">Welcome back</p><h1 className="mt-1 font-display text-5xl font-extrabold md:text-7xl">{profile.username}</h1><p className="mt-4 max-w-2xl text-white/60">Sharpen anime character recognition through progressive hints, ranked points, and persistent player growth.</p><div className="mt-8 max-w-2xl"><XPBar xp={profile.xp} nextLevelXp={xpToNextLevel(profile.level)} level={profile.level}/></div></GlassCard><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{stats.map(([Icon,label,value],i)=><motion.div key={label} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:i*.04}}><StatCard icon={Icon} label={label} value={value}/></motion.div>)}</div><GlassCard><h2 className="font-display text-2xl font-bold">Quick Actions</h2><div className="mt-5 grid gap-3 md:grid-cols-3">{[['/play','Play',Gamepad2,'Start a polished hint session.'],['/leaderboard','Leaderboard',Trophy,'Compare ranked points.'],['/profile','Profile',User,'Review your growth.']].map(([to,label,Icon,desc])=><Link key={to} to={to} className="group rounded-3xl border border-white/10 bg-white/[.04] p-5 transition hover:-translate-y-1 hover:border-anifine-primary/40 hover:bg-white/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-anifine-primary"><Icon className="text-anifine-accent"/><p className="mt-4 font-bold">{label}</p><p className="mt-1 text-sm text-white/50">{desc}</p></Link>)}</div></GlassCard></div> }
