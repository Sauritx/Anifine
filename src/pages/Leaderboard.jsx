import { useEffect, useMemo, useState } from 'react';
import GlassCard from '../components/ui/GlassCard'; import Avatar from '../components/ui/Avatar'; import Badge from '../components/ui/Badge'; import Input from '../components/ui/Input';
import { useAuth } from '../features/auth/AuthContext'; import { LeaderboardRepository } from '../repositories/LeaderboardRepository';
const fallback=[{uid:'a',username:'Akira',points:6420,level:12},{uid:'b',username:'Yuna',points:5110,level:10},{uid:'c',username:'Mika',points:3880,level:8},{uid:'guest',username:'Guest Player',points:1240,level:5}];
export default function Leaderboard(){ const {profile}=useAuth(); const [users,setUsers]=useState([]); const [loading,setLoading]=useState(true); const [search,setSearch]=useState(''); useEffect(()=>{LeaderboardRepository.top().then(u=>setUsers(u.length?u:fallback)).catch(()=>setUsers(fallback)).finally(()=>setLoading(false));},[]); const merged=useMemo(()=> users.some(u=>u.uid===profile.uid)?users:[...users,profile].sort((a,b)=>b.points-a.points),[users,profile]); const visible=merged.filter(u=>u.username.toLowerCase().includes(search.toLowerCase())); const currentRank=merged.findIndex(u=>u.uid===profile.uid)+1; return <GlassCard><Badge>Ranked Points</Badge><h1 className="mt-3 font-display text-3xl font-bold">Leaderboard</h1><div className="mt-5 max-w-md"><Input label="Search players" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Username"/></div>{loading ? <div className="mt-6 space-y-3">{[1,2,3,4].map(i=><div key={i} className="h-20 animate-pulse rounded-3xl bg-white/10"/>)}</div> : <><div className="mt-6 grid gap-3 md:grid-cols-3">{merged.slice(0,3).map((u,i)=><div key={u.uid} className="rounded-3xl border border-amber-300/20 bg-amber-300/10 p-5"><p className="text-sm text-amber-200">Top {i+1}</p><Avatar name={u.username} className="mt-4 h-14 w-14"/><p className="mt-3 font-display text-xl font-bold">{u.username}</p><p className="text-white/60">{u.points} pts · Level {u.level}</p></div>)}</div><div className="mt-6 space-y-3">{visible.length?visible.map((u)=><div key={u.uid} className={`grid grid-cols-[42px_1fr_auto] items-center gap-4 rounded-3xl border p-4 ${u.uid===profile.uid?'border-anifine-primary bg-anifine-primary/15':'border-white/10 bg-white/5'}`}><span className="font-display text-xl">{merged.findIndex(x=>x.uid===u.uid)+1}</span><div className="flex items-center gap-3"><Avatar name={u.username} className="h-11 w-11"/><div><p className="font-bold">{u.username}</p><p className="text-sm text-white/50">Level {u.level}</p></div></div><p className="font-display text-xl font-bold">{u.points}</p></div>):<p className="rounded-3xl border border-white/10 bg-white/5 p-6 text-white/55">No players match that search.</p>}</div>{currentRank>0 && <p className="mt-5 rounded-2xl bg-white/5 p-4 text-sm text-white/65">You are currently visible at rank <b className="text-white">#{currentRank}</b>.</p>}</>}</GlassCard> }
