import { useEffect, useState } from 'react';
import Card from '../components/Card'; import { useAuth } from '../context/AuthContext'; import { getLeaderboardUsers } from '../services/userService';
const fallback=[{uid:'a',username:'Akira',points:6420,level:12},{uid:'b',username:'Yuna',points:5110,level:10},{uid:'guest',username:'Guest Player',points:1240,level:5}];
export default function Leaderboard(){const {profile}=useAuth(); const [users,setUsers]=useState(fallback); useEffect(()=>{getLeaderboardUsers().then(u=>u.length&&setUsers(u)).catch(()=>setUsers(fallback))},[]); return <Card><h1 className="font-display text-3xl font-bold">Leaderboard</h1><div className="mt-6 space-y-3">{users.map((u,i)=><div key={u.uid} className={`grid grid-cols-[54px_1fr_auto] items-center rounded-2xl border p-4 ${u.uid===profile.uid?'border-anifine-primary bg-anifine-primary/15':'border-white/10 bg-white/5'}`}><span className="font-display text-xl">{i+1}</span><div><p className="font-bold">{u.username}</p><p className="text-sm text-white/50">Level {u.level}</p></div><p className="font-display text-xl font-bold">{u.points}</p></div>)}</div></Card>}
