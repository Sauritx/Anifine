import { Link, useNavigate } from 'react-router-dom'; import { useState } from 'react'; import Button from '../components/Button'; import Card from '../components/Card'; import { useAuth } from '../context/AuthContext';
export function Login(){return <AuthForm mode="login"/>} export function Register(){return <AuthForm mode="register"/>}
function AuthForm({mode}){const isRegister=mode==='register'; const {login,register}=useAuth(); const navigate=useNavigate(); const [form,setForm]=useState({username:'',email:'',password:''}); const [error,setError]=useState(''); const submit=async(e)=>{e.preventDefault();setError('');try{isRegister?await register(form):await login(form);navigate('/')}catch(err){setError(err.message)}}; return <div className="mx-auto max-w-md pt-10"><Card><h1 className="font-display text-3xl font-bold">{isRegister?'Create Account':'Login'}</h1><form onSubmit={submit} className="mt-6 space-y-4">{isRegister&&<input required placeholder="Username" onChange={e=>setForm({...form,username:e.target.value})} className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-anifine-primary"/>}<input required type="email" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})} className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-anifine-primary"/><input required type="password" minLength="6" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})} className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-anifine-primary"/>{error&&<p className="text-sm text-red-300">{error}</p>}<Button className="w-full">{isRegister?'Register':'Login'}</Button></form><p className="mt-5 text-center text-sm text-white/55">{isRegister?'Already have an account?':'Need an account?'} <Link className="font-bold text-anifine-accent" to={isRegister?'/login':'/register'}>{isRegister?'Login':'Register'}</Link></p></Card></div>}
